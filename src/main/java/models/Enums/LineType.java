package models.Enums;

/**
 * Enum representing the types of lines in the map file.
 */
public enum LineType {
    CONTINENT, // Represents a continent line
    COUNTRY,   // Represents a country line
    NEIGHBOR   // Represents a neighbor line
}
